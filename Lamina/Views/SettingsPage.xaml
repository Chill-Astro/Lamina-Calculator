<Page
    x:Class="Lamina.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Lamina.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    Padding="0"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>
    <Grid>
        <StackPanel x:Name="ContentArea" Margin="30,0,0,0">
            <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" />
            <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                <TextBlock Text="Theme" />
                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <RadioButton
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        Content="Light"
                        FontSize="15"
                        GroupName="AppTheme"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Light</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        Content="Dark"
                        FontSize="15"
                        GroupName="AppTheme"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Dark</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton
                        Command="{x:Bind ViewModel.SwitchThemeCommand}"
                        Content="Default"
                        FontSize="15"
                        GroupName="AppTheme"
                        IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default, Mode=OneWay}">
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Default</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                </StackPanel>
                <StackPanel Margin="0,10,0,0">
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="Backdrop Effect" />
                    <ToggleSwitch
                        Margin="0,5,0,0"
                        IsOn="{x:Bind ViewModel.IsMicaAltEnabled, Mode=TwoWay}"
                        OffContent="Acrylic"
                        OnContent="Mica Alt" />
                </StackPanel>
            </StackPanel>
            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="About" />
            <StackPanel Margin="{StaticResource XSmallTopMargin}">
                <HyperlinkButton Margin="{StaticResource SettingsPageHyperlinkButtonMargin}" NavigateUri="https://github.com/Chill-Astro/Lamina-Calculator/releases/latest">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="/Assets/Fonts/Lamina_Fonts.ttf#Lamina_Fonts"
                            FontSize="18"
                            Text="&#xe230;" />
                        <TextBlock
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" />
                    </StackPanel>
                </HyperlinkButton>
                <HyperlinkButton Margin="{StaticResource SettingsPageHyperlinkButtonMargin}" NavigateUri="https://github.com/Chill-Astro">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="/Assets/Fonts/Lamina_Fonts.ttf#Lamina_Fonts"
                            FontSize="18"
                            Text="&#xe90a;" />
                        <TextBlock
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            Text=" Chill-Astro" />
                    </StackPanel>
                </HyperlinkButton>
                <TextBlock x:Uid="Settings_BackgroundEffect" Style="{ThemeResource SubtitleTextBlockStyle}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
