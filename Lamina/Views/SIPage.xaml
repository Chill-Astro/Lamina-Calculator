<Page
    x:Class="Lamina.Views.SIPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{x:Null}">

    <Grid Background="Transparent">
        <StackPanel
            Margin="0,0,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Spacing="12">

            <InfoBar
                Title="Formula"
                IsClosable="False"
                IsIconVisible="True"
                IsOpen="True"
                Message="S.I. = (P × R × T) / 100&#10;Total = P + S.I."
                Severity="Informational" />

            <NumberBox
                x:Name="PrincipalNumberBox"
                Width="300"
                Header="Principal (P)"
                PlaceholderText="0.00"
                SpinButtonPlacementMode="Compact" />
            <NumberBox
                x:Name="RateNumberBox"
                Width="300"
                Header="Rate % (R)"
                PlaceholderText="0.00"
                SpinButtonPlacementMode="Compact" />
            <NumberBox
                x:Name="TimeNumberBox"
                Width="300"
                Header="Time in Years (T)"
                PlaceholderText="0.00"
                SpinButtonPlacementMode="Compact" />

            <Button
                x:Name="CalculateSIButton"
                Margin="0,15,0,0"
                HorizontalAlignment="Center"
                Click="CalculateSIButton_Click"
                Content="Calculate"
                Style="{StaticResource AccentButtonStyle}">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" />
                </Button.KeyboardAccelerators>
            </Button>
        </StackPanel>

        <ContentDialog
            x:Name="ResultDialog"
            Title="Result"
            DefaultButton="Primary"
            PrimaryButtonText="Close">
            <StackPanel Margin="0,10,0,0" Spacing="4">

                <TextBlock
                    x:Name="ResultLabel"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        x:Name="ResultValueText"
                        Grid.Column="0"
                        MaxWidth="400"
                        VerticalAlignment="Center"
                        FontSize="28"
                        FontWeight="SemiBold"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="Wrap" />

                    <Button
                        x:Name="CopyButton"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Click="CopyButton_Click"
                        Style="{StaticResource SubtleButtonStyle}">
                        <FontIcon Glyph="&#xE8C8;" />
                    </Button>
                </Grid>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>